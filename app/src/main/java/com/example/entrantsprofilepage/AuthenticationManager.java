package com.example.entrantsprofilepage;


// manages authentication for users
public class AuthenticationManager {

    // sign user in
    public void signUserIn(User user) {
        // if the user is valid (userId is in Firestore database)
        // AND device id has been used to sign in, accept
    }
}
